var a=[1,2,4,-7,-6]; 
var maxsum;
var cursum;
maxsum=cursum=0;
for(i=0;i<a.length;i++)
{
    cursum +=a[i];
   if(cursum<a[i])
   {
       cursum=a[i];
   }
   if(maxsum<cursum)
   {
       maxsum=cursum;
   }
    
}
console.log(maxsum);
